name: Milvasoft.Core Nuget Publish

on:
  push:
    branches: [ master ]
    paths:
      - 'Milvasoft.Core/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'Milvasoft.Core/**'

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
      - name: Restore Milvasoft.Core project dependencies
        run: dotnet restore Milvasoft.Core/Milvasoft.Core.csproj
      - name: Build Milvasoft.Core project
        run: dotnet build Milvasoft.Core/Milvasoft.Core.csproj --no-restore --configuration Release
      - name: Pack Milvasoft.Core project
        run: dotnet pack Milvasoft.Core/Milvasoft.Core.csproj --no-build --configuration Release --include-symbols -p:SymbolPackageFormat=snupkg -o .
      - name: Publish package to NuGet
        run: dotnet nuget push *.nupkg --api-key ${{secrets.NUGET_API_KEY}} --source "https://api.nuget.org/v3/index.json" --skip-duplicate
