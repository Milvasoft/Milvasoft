name: Milvasoft.Notification.Push.Expo Nuget Publish

on:
  push:
    branches: [ master ]
    paths:
      - 'src/Notification/Milvasoft.Notification.Push.Expo/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'src/Notification/Milvasoft.Notification.Push.Expo/**'

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 8.0.x
      - name: Restore Milvasoft.Notification.Push.Expo project dependencies
        run: dotnet restore src/Notification/Milvasoft.Notification.Push.Expo/Milvasoft.Notification.Push.Expo.csproj
      - name: Build Milvasoft.Notification.Push.Expo project
        run: dotnet build src/Notification/Milvasoft.Notification.Push.Expo/Milvasoft.Notification.Push.Expo.csproj --no-restore --configuration Release
      - name: Pack Milvasoft.Notification.Push.Expo project
        run: dotnet pack src/Notification/Milvasoft.Notification.Push.Expo/Milvasoft.Notification.Push.Expo.csproj --no-build --configuration Release --include-symbols -p:SymbolPackageFormat=snupkg -o .
      - name: Publish package to NuGet
        run: dotnet nuget push *.nupkg --api-key ${{secrets.NUGET_API_KEY}} --source "https://api.nuget.org/v3/index.json" --skip-duplicate
